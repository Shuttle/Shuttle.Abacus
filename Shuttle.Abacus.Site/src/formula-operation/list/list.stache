{{#if map.isResolved}}
    <h3>{{formula.name}}</h3>
{{/if}}

<abacus-formula-operation-item vm:formula:from="formula" vm:map:bind="operation" />

<cs-table vm:columns:from="columns" vm:rows:from="operations" />
