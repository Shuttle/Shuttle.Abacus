{{#if map.isResolved}}
    <h3>{{formula.name}}</h3>
{{/if}}

<abacus-formula-constraint vm:formula:from="formula" vm:map:bind="constraint" />

<cs-table vm:columns:from="columns" vm:rows:from="constraints" />
